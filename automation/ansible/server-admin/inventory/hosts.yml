all:
  children:
    debian_servers:
      hosts:
        azure_jump_server:
          ansible_host: jump.wetted.me
          ansible_user: azureuser
          ansible_ssh_private_key_file: ~/.ssh/azure-jumpserver.pem

    cloud_servers:
      hosts:
        azure_jump_server:

  vars:
    ansible_user: ansible
    ansible_ssh_private_key_file: ~/.ssh/homelab_ansible
