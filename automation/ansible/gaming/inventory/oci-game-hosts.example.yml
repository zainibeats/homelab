all:
  children:
    game_servers:
      hosts:
        oci_game_host:
          ansible_host: REPLACE_WITH_OCI_PUBLIC_IP
          ansible_user: debian
          ansible_ssh_private_key_file: ~/.ssh/oci-game-host
          ansible_python_interpreter: /usr/bin/python3
