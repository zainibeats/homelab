- name: Deploy Open WebUI Compose stack
  hosts: open_webui
  gather_facts: false
  become: true

  tasks:
    - name: Start Compose stack
      community.docker.docker_compose_v2:
        project_src: /home/ubuntu/ollama-aws-hybrid
        state: present
